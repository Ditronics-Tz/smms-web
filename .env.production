# =============================================================================
# Production environment (loaded by CRA automatically when NODE_ENV=production)
# Used by `npm run build` / `make prod`. Targets the real backend so production
# builds never silently point at localhost.
# =============================================================================
# MUST-SET-PER-DEPLOYMENT: fill in the real backend origin before building.
REACT_APP_API_BASE_URL=
